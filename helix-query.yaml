indices:
  mysite:
    include:
      - '**'
    exclude:
      - /documents/snippets/**
    target: /query-index
    properties:
      pagetitle:
        selectFirst: h1
        value: |
          textContent(el)
